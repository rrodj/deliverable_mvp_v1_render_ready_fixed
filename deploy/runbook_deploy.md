# Runbook — Deploy Inventory Guardian (MVP)

Generated: 2025-11-06T06:24:30Z

From this directory:
```bash
docker compose -f docker-compose.yml build
docker compose -f docker-compose.yml up -d
```
Then open http://localhost:8080
